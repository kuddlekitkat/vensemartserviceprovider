import 'package:flutter/material.dart';

class CompletedRequestList extends StatelessWidget {
  const CompletedRequestList({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
